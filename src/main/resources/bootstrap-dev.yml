server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wyze-scale?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&autoReconnect=true
    username: root
    password: mysqladmin
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 1
      min-idle: 5
      max-active: 10
      max-wait: 3000
#  redis:
#    host: 127.0.0.1
#    port: 6379
#    database: 6

aws:
  region: us-west-2
  accessKey: fakeMyKeyId
  secretKey: fakeSecretAccessKey
  s3:
    region: us-west-2
    base_url: https://s3.us-west-2.amazonaws.com.cn/
    resource:
      bucket_name: wyze-resource
    expire: 600
  sns:
    export-sns-topic: exportSnsTopic

wyze:
  log:
    stdout:
      enable: true
  scale:
    service:
      export-max-time: 10